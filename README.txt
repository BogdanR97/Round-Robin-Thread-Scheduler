=== Brief Description ===
A dynamic library implemented in C for Linux-based Operating Systems which controls the execution of multiple threads in the user space. The library simulates a preemptive process scheduler in a single-processor system that uses a priority based Round Robin CPU Scheduling Algorithm.

=== Compiler Instructions ===

To create the library you  have to run:
	make libscheduler.so
